export enum SpoilerLevels {
  PRERENDER = "ssg",
  STRICT = "strict",
  SOME = "some",
  ALL = "all"
}

export enum Availability {
  PERMANENT,
  STORYLOCKED,
  LIMITED,
  WELFARE,
  FP_POOL,
  FP_LIMITED,
  FP_LOCKED
}
