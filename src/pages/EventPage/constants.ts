import { join } from "path";

export const EventAssetsDir = join("assets", "data", "events-new"); // PLACEHOLDER: change to events when migrating prod
// TODO: there is probably a better way to store these paths, just gotta make sure nft sees them :)
